/*let userInputOne = prompt('enter a string');
if (userInputOne.length < 4) {
    prompt("gotta be four letters at least")
};
for(let i = 3; i < userInputOne.length; i++) {
    console.log(userInputOne[i]);
};
*/

/*let max = 10;
let randomNumber = Math.floor(Math.random() * max) +1;
console.log(randomNumber);
let userGuess = parseInt(prompt('guess a number between 1 and 10'));
while(randomNumber != userGuess) {
    userGuess = prompt('Sorry try again')
}
alert(`Yay you got it! It was ${userGuess}`);
*/

/*let max = 10;
let userGuess;
let numGuesses = 0;
let randomNum = Math.floor(Math.random() * max) + 1;
while (randomNum !== userGuess) {
    userGuess = parseInt(prompt(`${randomNum} Guess a random number between 1 - 10`));
    numGuesses++;
}; */

/*for (let i = 1; i <= 5; i++) {
    let row = "";
    for (let j = 1; j <= i; j++) {
        row += "*"
    }
    console.log(row);
}
*/

/*let counter = -1;
let coin;

while (coin !== 0) {
    counter++;
    coin = Math.floor(Math.random() * 2);
    console.log(coin);
}

console.log(`It took ${counter} retries to get heads!`); */









